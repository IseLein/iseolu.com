---
import BaseLayout from "./BaseLayout.astro";
import "../styles/markdown.css";

const { frontmatter } = Astro.props;
---
<BaseLayout pageTitle={frontmatter.title} image={frontmatter.image.url} description={frontmatter.description}>
    <slot />
</BaseLayout>